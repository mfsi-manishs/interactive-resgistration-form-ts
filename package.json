{
  "name": "interactive-resgistration-form-ts",
  "version": "1.0.0",
  "description": "A single-page web application using **HTML, CSS, and Vanilla JavaScript** that allows users to **register details using a form** and **manage those records dynamically** in a table without page reloads and no libraries.",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "copy": "cpy \"src/**/*.{html,css}\" dist --parents",
    "build": "npm run clean && npm run copy && tsc",
    "dev": "npm run copy && tsc --watch",
    "serve": "npx serve dist",
    "start": "npm-run-all --parallel dev serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpy-cli": "^6.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.1.2",
    "serve": "^14.2.5",
    "typescript": "^5.9.3"
  }
}
